import React from 'react';
import { Link } from "react-router-dom";

const EfemeridesPage = (props) => {
    return (
        <div>Efemerides pagina</div>    
    
    );
};
export default EfemeridesPage;